syntax = "proto3";
package alice.checkers.checkers;

option go_package = "github.com/alice/checkers/x/checkers/types";

message SystemInfo {
  uint64 nextId = 1; 
  string fifoHeadIndex = 2; // index of the game at the head
  string fifoTailIndex = 3; // index of the game at the tail
}
